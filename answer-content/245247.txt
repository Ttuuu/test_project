<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>When I run the game, it allows me to select a side. I selected 'x'. Then the display changed to add the 'o' side, then the display commenced to flash with no (discernible) way for me/computer to make a move.</p>
<p>suggest the <code>system( "clear" );</code> only be called after a side inputs a move.</p>
    </div>