<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>You should use:</p>

<pre><code>guess = int((hborder + guess) / 2)
</code></pre>

<p>instead of <code>guess = round((hborder + guess) / 2)</code> for correct computation.</p>
    </div>