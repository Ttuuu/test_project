<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>Why have <code>main()</code> be a single line calling a single function? Either rename <code>main()</code> to <code>get_xkcd()</code> or just call <code>get_xkcd()</code>.</p>

<p>Also, maybe use <code>BeautifulSoup</code> instead of <code>lxml</code>.</p>
    </div>