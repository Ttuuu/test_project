<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>In <code>sum_water_refractor</code> you should use <code>len</code> instead of <code>lambda x: len(x)</code>. There is no point in using a lambda if you don't change anything (number or value of parameters).</p>
    </div>