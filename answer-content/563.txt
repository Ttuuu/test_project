<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>It appears that the method sometimes returns a timestamp (<code>time</code>) and sometimes it returns a duration (difference between timestamps) (assuming that <code>time</code> is always a timestamp - if not, then this issue simply moves to <code>time</code> having a dual definition).</p>
    </div>