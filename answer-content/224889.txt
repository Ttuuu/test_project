<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>I can add that instead of <code>DisplayLinklist()</code> you should override <code>toString()</code> that returns a <code>String</code> representation of the linked list. then the caller can decide where and how to display this <code>String</code>.</p>
    </div>