<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>A faster alternative to writing the values individually is to assign the Variant array to the <code>Value</code> property of the top left cell of the range you want to past it into. That writes the array in one go.</p>

<p>This will require using a second array to invert the ordering in case <code>fromTop</code> is <code>False</code>. </p>
    </div>