<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p><code>MainWindow</code> is a view object which should be avoided in <code>ViewModel</code> classes. Passing the collection as constructor parameter to the <code>UpdateViewModel</code> would be better. If you want to achieve a high level of testability, you would need to study more about dependency injection.</p>
    </div>