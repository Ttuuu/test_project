<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>The rewrite of LocalStorage is normal practice because of a rewrite time ~equal check time. </p>

<p>You can change the code.</p>

<pre><code>localStorage.setItem('lang', language);
</code></pre>

<p>By the way <code>JSON.parse</code> and  <code>JSON.stringify</code> is unnecessary actions in your code because <code>language</code> is already <code>string</code>.</p>

<hr>

<p><em>I only considered the code associated with your the question.</em></p>
    </div>