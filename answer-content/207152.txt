<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>Since <code>myDictionary</code> values are declared as <code>Any</code>, you could have anything (or even nothing) assigned at "scores", so you need to safe-cast the value in to a variable first and then reassign it.</p>

<p>I don't know about Python, but Swift doesn't check dictionary values at compile time.</p>
    </div>