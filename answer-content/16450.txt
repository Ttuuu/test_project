<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p><code>topOfStack</code> is always one less than size, so you don't need that variable. Just replace all instances with <code>size-1</code>. Similarly, <code>capacity</code> could be replaced with <code>theData.length</code>.</p>
    </div>