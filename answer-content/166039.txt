<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>Just remove <code>else:</code> and unindent <code>return True</code>. That should work since <code>if</code> and <code>elif</code> will always return if entered.</p>
    </div>