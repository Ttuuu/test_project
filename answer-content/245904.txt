<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>The code looks perfect, except for the variable name <code>numbers</code>. This variable only holds a single number, therefore its name must be <code>number</code> instead.</p>
<p>Since the variable is only used in a very small scope, the names <code>n</code> or <code>i</code> may be acceptable as well. (Subject to personal preference.)</p>
    </div>