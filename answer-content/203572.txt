<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>The only benefit of using <code>__invoke()</code> is that you can store the instance of the class in a variable as a callback and then use it without <code>call_user_func_array()</code>. But in my opinion using <code>create()</code> is more readable so you can guess easily what is the code doing.</p>
    </div>