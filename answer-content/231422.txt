<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>Two years have passed but your wrap is really a good start for wrapping indexedDb with rxjs. <code>Take(1)</code> now lives in rxjs/operators import and must be used with a pipe but besides this needed an upgrade.</p>

<p>Your approach with rxjs seems like the right choice when handling indexedDb without external libraries. Thumbs up!</p>
    </div>