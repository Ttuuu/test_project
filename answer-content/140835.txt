<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>What I understand is that you want to know if 2 strings contain the same characters in the same quantity and not necessarily arranged similarly.</p>

<p>For example <code>"cat"</code> and <code>"act"</code> should return <code>true</code>.</p>

<p>You could check the length, then sort both strings and just compare them using <code>==</code>.</p>
    </div>