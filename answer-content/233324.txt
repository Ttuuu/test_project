<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>Nesting <code>if</code> statements isn't considered as clean code. I advise you'd use <code>bouncer</code> design pattern. Check your constraints first. If they're are not satisfied, just return.</p>
    </div>