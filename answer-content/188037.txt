<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>I got a question about the code, why use <code>\s</code> and not just enter a space and use <code>\d*,</code>? </p>

<p>What I mean is,</p>

<pre><code>(r'Average = \d*',i)
</code></pre>

<p>Instead of </p>

<blockquote>
<pre><code>(r'Average =\s\d*,i)
</code></pre>
</blockquote>

<p>Why use <code>\s</code>? If we did not use it before the <code>=</code> sign?</p>
    </div>