<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>My first question seeing your code is if all conditions are mutually exclusive. In other words, do you want the code inside the <code>else</code> block to be used if the <code>if</code> condition is not met? Or it would be fine to go to the <code>else</code> code regardless if the first condition was met.</p>

<p>I also recommend following these guidelines to avoid this kind of code <a href="https://blog.codinghorror.com/flattening-arrow-code/" rel="nofollow noreferrer">flattening arrow code</a></p>
    </div>