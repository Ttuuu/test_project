<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>Most rectangles have a length and a width.  I’ve never heard of a rectangle with a radius, but it would be known as a square.</p>

<p>More constructively:</p>

<pre><code>System.out.print(String.format("(%d,%d) ", x - r + i, y - r));
</code></pre>

<p>Could be written as:</p>

<pre><code>System.out.format("(%d,%d) ", x - r + i, y - r);
</code></pre>
    </div>