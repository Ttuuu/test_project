<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>This function is not just cryptic — it's also wrong.  I would expect <code>convert_to_24('12:00:00AM')</code> (midnight) to be <code>'00:00:00'</code>, but it returns <code>'12:00:00'</code> (noon).</p>
    </div>