<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>A one liner:</p>
<pre><code> np.sum(r_array*va_array, axis=1, keepdims=True)
</code></pre>
<p>To match <code>r_array@va_array</code>, use <code>va_array.T</code> in the 1liner.</p>
    </div>