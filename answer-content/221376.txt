<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>You can use:</p>

<pre><code>dict.ContainsKey(key) ? dict[Key] : defaultValue
</code></pre>

<p>so you don't need to declare <code>TValue</code>.</p>
    </div>