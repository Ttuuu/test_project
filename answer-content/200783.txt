<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>You would also have to add this to the <code>findAllPairs</code> method to sort the initial list which is the input:</p>

<pre><code>Arrays.sort(input);
</code></pre>
    </div>