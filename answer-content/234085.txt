<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>When you run into an exception, you could use <code>raise</code> (by itself) instead of <code>sys.exit()</code> to re-raise the exception. This way the user can see the stack trace when there's a failure and any (hypothetical) other consumers of <code>fetch_question_markdown</code> can catch the exception if appropriate.</p>
    </div>