<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p><code>1 + 2 + ... + n = n * (n + 1) / 2</code>, for example, <code>1 + 2 + 3 = 3 * (3 + 1)/2 = 6</code>,
so the <span class="math-container">\$O(N)\$</span> is that:</p>

<ol>
<li>sum the array, O(n)  (no sorting)</li>
<li>check sum == n * (n + 1) / 2</li>
</ol>
    </div>