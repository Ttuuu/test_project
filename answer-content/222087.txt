<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>if you're just trying to decide between <code>number--high</code> <code>number--medium</code> or <code>''</code> based on the <code>condition</code> variable, then just make a helper function that does that</p>

<p>then do <code>return &lt;div className={"number " + getNumberClass(condition)}}&gt;{number}&lt;/div&gt;</code></p>
    </div>