<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>One more hint in addition to the other answers:</p>

<p>You can completely remove the <code>size</code> field. It's meant to be the array length, so internally you can refer to <code>stackArray.length</code> instead and avoid the risk of inconsistency between size and array length.</p>
    </div>