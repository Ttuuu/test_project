<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>It doesn't look too bad. To add to the other answers:</p>

<ul>
<li>Please put a space after the if keyword.</li>
<li>If you are not going to use javadoc then remove the comments.</li>
<li>Joining SQL strings is usually a very bad practice because it can make you vulnerable to SQL injection. Now if this really is an issue is impossible to tell, because you didn't include the actual queries.</li>
</ul>
    </div>