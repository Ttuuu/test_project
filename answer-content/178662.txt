<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>First I recommend you to use a framework like <code>symfony</code>, <code>laravel</code>, <code>silex</code>, etc.</p>

<p>In <code>entrar.php</code>, you are doing different things, please move them to readable separated files.</p>

<p>For admin users, you can easily add an <code>is_admin</code> column and stores it in session instead of using your profile id.</p>
    </div>