<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>Small details:</p>

<ul>
<li><p>It's confusing that you define <code>NUM_NAMES</code> as a macro and then <code>names</code> as a variable.  Either define two variables or two macros, but keep those two items together.</p></li>
<li><p>If you use <code>const</code>, use it everywhere (<code>printNames</code> too).</p></li>
</ul>
    </div>