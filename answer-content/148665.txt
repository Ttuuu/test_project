<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>What about modifying the return of <code>_fetch()</code>? Inside the <code>if</code> statement that checks you could have it return <code>false</code> or a custom error code at that point. And then back one scope, catch the return of <code>_fetch()</code> and test it against your error code or <code>false</code> then just handle it appropriately. Either escaping out with <code>return</code> or reauthorizing in between.</p>
    </div>