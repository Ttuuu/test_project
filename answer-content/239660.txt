<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>Your function isn't returning anything. You should be building a string and returning it from your function. <code>str.join()</code> will be useful for building your string, return it with the <code>return</code> keyword. </p>
    </div>