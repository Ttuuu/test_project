<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>You should try to implement <code>min</code> and a minimal <code>typelist</code> to get rid of <code>std::tuple</code> and <code>std::min</code> (and their headers) and so, make it compatible with c++11 and reduce header dependencies.</p>

<p>but otherwise it's a pretty useful trait,  well writed and easy to understand.</p>
    </div>