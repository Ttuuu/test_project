<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>You are representing each state of the universe as a list of bits. However, CPUs are experts at handling sequences of bits — that's what an integer is!</p>

<p>Given two bits, the way to find out whether exactly one of them is 1 is to use the <code>^</code> operator (XOR).</p>

<p>You should be able to solve this challenge using bit-shifting (<code>&lt;&lt;</code> and <code>&gt;&gt;</code> operators) and <code>^</code>.</p>
    </div>