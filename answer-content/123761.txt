<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>One should note that a Stream can never really be an Iterable because it can't be invoked more than once.  However, if you are certain that the Stream will only be iterated once, to create an Iterable from a stream, one only need to use the expression <code>myStream::iterator</code></p>
    </div>