<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>There's not much to review; that said, the input is guaranteed to be
between zero and \$2^{32}\$, so there is no point to the <code>N &amp; 0xffffffff</code> really.  Also the <code>print</code> statement would be more upwards
compatible with Python 3 if it was used like a function,
i.e. <code>print(...)</code>.</p>
    </div>