<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>Can you use a stream rather than an array in the rest of your application? If so I'd simply cut off the <code>toArray</code> bit at the end. Otherwise this looks exactly like what I'd expect in Java 8.</p>
    </div>