<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>I'd rename <code>OnAccept</code> to something like <code>BeginAccept_callback</code>.  You will mainly see the <code>OnSomething</code> naming convention for virtual functions.  Case in point- <code>System.Windows.Forms.Control</code> is chock full of virtual functions where the base implementation is to raise an event.  Likewise for the other callback handlers.</p>
    </div>