<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>A couple of things other answers haven't touched upon.</p>

<ul>
<li>Your <code>main</code> function is missing its <code>return</code> statement.</li>
<li><code>exit(int status)</code> should be avoided for normal program flow, use <code>return</code> instead.</li>
</ul>
    </div>