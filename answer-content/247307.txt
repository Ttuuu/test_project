<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>This looks good and well thought through.</p>
<p>The <code>continue</code>, <code>break</code> and <code>return</code> statements are always a bit tricky, it might be useful to add a comment to them to explain what they continue/break/return from.</p>
<p>Theoretically we could be in an endless loop if the computer player never finds a non-full tile.... , but the AI algorithm you planned will fix that :-)</p>
    </div>