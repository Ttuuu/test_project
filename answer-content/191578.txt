<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>Just an Information, which helps with either the unity tutorial EventManager or the one posted above:</p>

<p>Use the <code>StartListening</code> call in <code>Awake()</code> and the <code>StopListening</code> in <code>~YourClass()</code> to have disabled GameObjects listen to events.</p>

<p>(This is an answer not a comment, because I cannot post comments yet.)</p>
    </div>