<div class="s-prose s-prose__disable-spoiler-hover js-post-body" itemprop="text">
<p>It's OK, just a few comments:</p>

<p>For <code>RUNNING6MPH</code>, etc., an <code>enum</code> seems to be optimal.</p>

<p>I'd recommend to start with all methods <code>private</code>. Only when you need them, you review them and then change the visibility.</p>

<p>For the time conversion, there's <code>TimeUnit</code>.</p>
    </div>